import Section from '@/components/ui/section';
import SelectedWorkList from '@/components/ui/selected-work';

/** Opens the resume: what was built, before what the titles were. */
const SelectedWork = () => (
  <Section id="selected-work" title="Selected work">
    <SelectedWorkList />
  </Section>
);

export default SelectedWork;
